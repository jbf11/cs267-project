#!/bin/bash
#SBATCH -A mp309
#SBATCH -N 1
#SBATCH -C knl
#SBATCH -q debug
#SBATCH -J fix
#SBATCH --ntasks-per-node=8
#SBATCH -t 00:30:00

#OpenMP settings:
# export OMP_NUM_THREADS=1
# export OMP_PLACES=threads
# export OMP_PROC_BIND=spread

#run the application:
srun -n 8 ./fix-flux -use_mumps_lu -v -nt 500
